import React from 'react';

function Extensions() {
    // add built in theme picker and change tailwind scheme via custom themes (light/dark mode)
    return (
        <div className="flex flex-row bg-zinc-800 w-64 text-white justify-center h-screen">
            <p>🚧 Extensions work in progress 🚧</p>
        </div>
    );
}

export default Extensions;